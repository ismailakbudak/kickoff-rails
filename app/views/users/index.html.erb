<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
    
    <!--- Bu satırların yerine sadece alttaki satır kullanılabilir _userhtml.erb sayesinde
        <% @users.each do |user| %>
            <li>
                <%= gravatar_for user, size: 52 %>
                <%= link_to user.name, user %>
            </li>
        <% end %>
   -->
  <%= render @users %>
</ul>

<%= will_paginate %>